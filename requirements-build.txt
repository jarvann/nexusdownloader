# Build and packaging dependencies for NexusDownloader
# Use: pip install -r requirements-build.txt

# PyInstaller for creating executables
# NOTE: v6 removed bytecode encryption and the [encryption] extra; spec files
# must drop block_cipher and onedir output now lives under dist/<app>/_internal/.
pyinstaller>=6.20.0

# Additional packaging tools
wheel>=0.47.0
setuptools>=82.0.1

# Windows-specific build dependencies
pywin32>=311; sys_platform == "win32"
pywin32-ctypes>=0.2.3; sys_platform == "win32"

# Linux-specific build dependencies
distro>=1.9.0; sys_platform == "linux"

# Development tools
twine>=6.2.0  # For uploading to PyPI if needed